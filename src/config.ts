// The application name to be displayed in the document title.
export const APP_NAME = process.env.REACT_APP_NAME || 'Asatelit POS';

// The path to the data that is used to run the application in demo mode.
export const DEMO_DATA_PATH = process.env.REACT_APP_DEMO_DATA || null;
